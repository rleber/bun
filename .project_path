#!/bin/bash

# Project-specific path setting. Add pathr commands to this file, to add specific path settings for the 
# current project, e.g.:

# pathr add -s project --end /this/is/an/executable/path
#
# These commands should be inserted where shown

project_dir=`dirname "$BASH_SOURCE"`
pathr add -s project --end "$project_dir/src"
# <<---- Insert additional path preferences here
